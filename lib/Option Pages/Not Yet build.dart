import 'package:flutter/material.dart';

class NotYetBuild extends StatelessWidget {
  const NotYetBuild({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("not yet build"),
        centerTitle: true,
      ),
    );
  }
}
